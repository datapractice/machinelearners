```{r lit}
library(scilitlearn)
library(dplyr)
library(ggplot2)

wos = load_data('support/gov.tsv')

g = terms_over_time(wos, plt=TRUE, terms_to_plot = 30)
g
View(title_word_count(wos))
View(title_bigrams_filtered(wos))
wos %>% select(PY, DT) %>% group_by() %>% count(PY)

keyword_count_top_plot(wos, 2)
cited_reference_count(wos)  
authors_top(wos)

peak_terms_monthly(wos,combine=FALSE, field='DE',  terms_to_plot= 30)
cited_references_time(wos)
```

t = cited_reference_count(wos)  
tc = t %>% group_by(ref) %>% mutate(cu = cumsum(n))
tct = tc %>% top_n(n=20, wt = cu)
ggplot(tct, aes(x=as.Date(PY, format = '%Y'), y=n)) + geom_col() + facet_wrap(~ref) + scale_x_date()
tct[29,]
